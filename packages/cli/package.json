{
	"name": "flipagent-cli",
	"version": "0.0.2",
	"description": "flipagent CLI — log in, drive the API from your shell (search / sold / evaluate / ship / buy / sales / payouts / transactions / messages / offers / disputes / feedback), diagnose your host with `doctor`, and one-command-set-up the flipagent MCP server for Claude Code and other MCP hosts. Buy-side execution (/v1/purchases) runs in two transports — REST passthrough (with eBay Buy Order API approval) or the flipagent Chrome extension. Zero runtime deps.",
	"type": "module",
	"main": "./dist/index.js",
	"bin": {
		"flipagent-cli": "./dist/index.js"
	},
	"files": [
		"dist",
		"README.md",
		"LICENSE"
	],
	"scripts": {
		"clean": "shx rm -rf dist *.tsbuildinfo",
		"build": "tsc -p tsconfig.build.json && shx chmod +x dist/index.js",
		"dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput"
	},
	"devDependencies": {
		"@types/node": "^22.10.5",
		"shx": "^0.4.0",
		"typescript": "^5.9.2"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"keywords": [
		"flipagent",
		"mcp",
		"cli",
		"agent",
		"claude",
		"cursor",
		"ebay",
		"reselling"
	],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/flipagent/flipagent.git",
		"directory": "packages/cli"
	},
	"publishConfig": {
		"access": "public"
	}
}
