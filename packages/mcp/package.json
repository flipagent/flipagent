{
	"name": "flipagent-mcp",
	"version": "1.0.0",
	"description": "MCP server exposing flipagent's reseller toolchain (eBay search, sold-listing search, evaluate, buy, list, ship, sale fulfillment, finance) to Claude Desktop, Cursor, Cline, Zed, Continue, Windsurf, and any MCP-compatible AI client. Toolsets opt-in via FLIPAGENT_MCP_TOOLSETS for comms, forwarder, notifications, seller_account, admin. Talks to api.flipagent.dev by default; point FLIPAGENT_BASE_URL elsewhere for self-host / staging.",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"bin": {
		"flipagent-mcp": "./dist/index.js"
	},
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		},
		"./http": {
			"types": "./dist/http.d.ts",
			"default": "./dist/http.js"
		},
		"./server-factory": {
			"types": "./dist/server-factory.d.ts",
			"default": "./dist/server-factory.js"
		},
		"./tools": {
			"types": "./dist/tools/index.d.ts",
			"default": "./dist/tools/index.js"
		},
		"./config": {
			"types": "./dist/config.d.ts",
			"default": "./dist/config.js"
		}
	},
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"clean": "shx rm -rf dist *.tsbuildinfo",
		"build": "tsc -p tsconfig.build.json && shx chmod +x dist/index.js",
		"dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
		"test": "vitest --run"
	},
	"dependencies": {
		"@flipagent/types": "^1.0.0",
		"@flipagent/sdk": "^1.0.0",
		"@modelcontextprotocol/sdk": "^1.0.0",
		"@sinclair/typebox": "^0.34.41"
	},
	"devDependencies": {
		"@types/node": "^22.10.5",
		"shx": "^0.4.0",
		"typescript": "^5.9.2",
		"vitest": "^3.2.4"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"keywords": [
		"mcp",
		"model-context-protocol",
		"claude",
		"cursor",
		"ebay",
		"deal-finding",
		"arbitrage",
		"flipagent"
	],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/flipagent/flipagent.git",
		"directory": "packages/mcp"
	},
	"publishConfig": {
		"access": "public"
	}
}
